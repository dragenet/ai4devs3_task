export interface ScrapedData {
    question: string;
}

export interface ProcessedData {
    answer: string;
}

export interface FormData {
    username: string;
    password: string;
    answer: string;
} 